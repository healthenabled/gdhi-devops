---
  - name: Download GDHI_App Artifact
    unarchive:
      src: "{{ artifact_app_url }}"
      dest: "{{deploy_dir}}"
      remote_src: yes
      mode: 07771
      extra_opts: [--strip-components=1]
    become: yes
